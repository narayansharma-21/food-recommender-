spring:
  datasource:
    url: jdbc:h2:mem:food_recommender;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate

platform:
  jobs:
    enabled: false
